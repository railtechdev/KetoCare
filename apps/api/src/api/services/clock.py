"""Местное «сегодня» для API.

Сама функция живёт в `core.clock`: её читают и репозитории (правило «исходная
частота пишется, пока терапия не началась»), и сервисы API, и оба обязаны
считать «сегодня» одинаково. Здесь только переэкспорт — чтобы вызывающие в
`apps/api` не тянули `core` напрямую и чтобы тесты по-прежнему подменяли
`api.services.intake.local_today` там, где подменяли.
"""

from __future__ import annotations

from core.clock import local_today

__all__ = ["local_today"]
