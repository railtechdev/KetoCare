/**
 * Ширина боковой панели и отступ содержимого — одно решение, записанное один
 * раз.
 *
 * Панель `fixed`, и содержимое отодвигается от неё отдельным классом. Разойдись
 * эти два числа — содержимое уедет под панель, и заметить это можно только
 * глазами на широком экране. Пары объявлены рядом и проверяются тестом
 * (`layout.test.ts`).
 */
export const NAV = {
  /** Полоса значков с 768 px, подписи — с 1024 px. */
  sections: { aside: "w-16 lg:w-64", content: "md:pl-16 lg:pl-64" },
  /**
   * Внутри карты пациента — только значки, на любой ширине.
   *
   * Два полных меню рядом съели бы 30rem ширины и спорили бы, которое из них
   * главное. Сжатие при этом не только освобождает место: это единственный
   * видимый признак того, что врач ушёл уровнем глубже, — и он же обещает, что
   * подняться обратно можно тем же меню, а не «Назад» браузера.
   */
  patient: { aside: "w-16", content: "md:pl-16" },
} as const;
