/**
 * Задержка автоматического пересчёта.
 *
 * Правка граммовки — это несколько нажатий подряд; без задержки каждое
 * уходило бы в расчёт. Та же величина, что у поисковых полей. Отдельным
 * модулем — её читают и экран, и тесты, которым нужно дождаться, что лишняя
 * проверка точно успела бы уйти.
 */
export const AUTO_CALC_DELAY_MS = 400;
