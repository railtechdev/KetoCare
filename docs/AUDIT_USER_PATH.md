# Аудит бизнес-логики и пути пользователя

Дата: 2026-08-28. Проверялось по коду на ветке `feat/stage1-foundation` и по
`docs/TZ_AI_AGENTS.md`. Каждый пункт подтверждён ссылкой на файл или строку ТЗ, а не
предположением.

Столбец «Источник»: **ТЗ** — требование уже есть в ТЗ и не выполнено; **пробел** — ТЗ
темы не содержит и раздел 16 её не исключает, решение принято отдельным ADR.

## Блокирующее: продукт не работает без этого

**Закрыто** — коммиты `831a006`, `a1583ff` и последующий по приглашениям.

| # | Что | Источник | Доказательство |
|---|---|---|---|
| 1 | Врача нечем привязать к пациенту → список пациентов у врача пуст всегда, весь кабинет недостижим | пробел → [ADR-0003](adr/0003-onboarding-and-patient-links.md) | `link_doctor` вызывает только `infra/scripts/seed_demo.py`; в таблице ручек §5.3 записи нет |
| 2 | Нет интерфейса приглашения и его принятия → нового родителя, врача или диетолога невозможно завести | пробел → [ADR-0003](adr/0003-onboarding-and-patient-links.md) | ручки `POST /auth/invitations` и `/accept` есть; во фронтенде слова `invitation` нет нигде, кроме списка значений фильтра аудита |
| 3 | Нет формы добавления ребёнка | пробел → [ADR-0003](adr/0003-onboarding-and-patient-links.md) | `POST /api/v1/patients` не вызывается из `apps/web` |
| 4 | Родитель с двумя детьми не видит ничего | пробел → [ADR-0003](adr/0003-onboarding-and-patient-links.md) | `useCurrentPatientId` возвращает `null`, если детей не ровно один |

## Требования ТЗ, которые не выполнены

| # | Что | Строка ТЗ | Состояние |
|---|---|---|---|
| 5 | ~~`/patients` описан как CRUD, реализованы только create и read~~ | §5.3 | **закрыто:** `PATCH /patients/{id}` + форма в разделе «Настройки» |
| 6 | «сессии/refresh — ревокация при смене пароля» | §11 | смены пароля нет вовсе, ревокации нет; забывший пароль родитель заперт навсегда, угнанную сессию нечем погасить |
| 7 | Раздел `settings` есть в навигации родителя | §8.1 | экрана нет, и в §8.3 у него нет критериев приёмки — состав раздела не определён |
| 8 | Диетолог видит раздел «Пациенты», но `medical-profile` и заметки на сервере doctor-only | §8.1 vs `routers/clinical.py` | объём доступа диетолога не утверждён, см. вопрос 7 в `medical/OPEN_QUESTIONS.md` |

## Пробелы, закрытые решением

| # | Что | Решение |
|---|---|---|
| 9 | `recipes.photo_path` есть в модели, загрузить фото нечем | [ADR-0004](adr/0004-files-and-attachments.md) |
| 10 | Вложений (выписка, ЭЭГ, анализы, бумажное назначение) нет ни в модели, ни в ручках | [ADR-0004](adr/0004-files-and-attachments.md) |

## Замечено, решения не требует сейчас

- **Доставки приглашения нет** (ни SMTP, ни его переменных в §12). По бенчмарку
  ([ADR-0003](adr/0003-onboarding-and-patient-links.md)) доступ выдаётся на приёме,
  поэтому достаточно показать администратору или врачу готовую ссылку. Если появится
  требование рассылать письма — это отдельная тема с почтовым провайдером в стеке.
- **Отвязать родителя от ребёнка нечем** (смена опеки, ошибка привязки). Ручка
  отвязки врача в ADR-0003 есть, родителя — нет: снятие доступа к ребёнку у родителя
  затрагивает больше, чем техника, и должно опираться на правило клиники.
- **Уведомление семьи о новом назначении** помечено в коде как TODO этапа 3
  (`routers/prescriptions.py`), поэтому баннер врача сокращён до «Создана версия N»
  без обещания «семья уведомлена» из §8.3.

## Что подтвердилось верным

- Связи `parent_patient` и `doctor_patient` — многие-ко-многим с `unique(пара)`. Это
  правильная модель: у ребёнка бывает двое родителей, у родителя — двое детей, у
  пациента — несколько специалистов. Схему менять не нужно, вопрос был только в
  интерфейсе и в отсутствующей ручке привязки.
- Разграничение доступа проверяется на сервере в каждой ручке с данными пациента, и
  администратор к клиническим данным доступа не имеет — проверено интеграционными
  тестами `apps/api/tests/test_access_control.py`.
