# Материалы заказчика

Сюда кладутся исходники от заказчика и медицинской команды: спецификации,
выгрузки, брендбук, примеры отчётов, уточнения к требованиям.

## Куда что класть

| Что прислали | Куда | Почему |
|---|---|---|
| Медицинская спецификация расчёта, эталонные случаи, клинические протоколы | **`docs/medical/`** | раздел 0.1 ТЗ называет `docs/medical/calculation-engine-spec.md` источником истины; каталог защищён хуком — агент читает, но не правит (правила 1 и 2 `CLAUDE.md`) |
| Всё остальное: требования, брендбук, макеты, примеры отчётов, выгрузки справочников | **`docs/client/`** | эта папка |
| **Что угодно с реальными данными пациентов** — выписки, анализы, ФИО, контакты | **`docs/client/private/`** | папка в `.gitignore`: агент прочитает, в репозиторий не попадёт |

`docs/client/private/` — не «секретная», а **не попадающая в git**. Репозиторий
на GitHub, а раздел 16 ТЗ прямо запрещает коммитить реальные ФИО. Если не
уверены, что в файле — кладите туда, разберём и переложим.

## Форматы

Читаются как есть: `.md`, `.txt`, `.csv`, `.json`, `.yaml`, `.pdf`,
изображения (`.png`, `.jpg`).

Требуют преобразования (делается на месте, `textutil` встроен в macOS):
`.doc`, `.docx`, `.rtf` → текст. Для `.xlsx` удобнее выгрузка в CSV — так
видно, где заканчиваются данные и начинается оформление.

Разобранный текст присланных документов лежит в **`extracted/`**: оригинал —
источник истины, разбор — чтобы не конвертировать заново в каждой задаче.

## Указатель

### Поступление 29.08.2026

| Файл | Что это | Состояние |
|---|---|---|
| `ПЕРЕЧЕНЬ_ВОПРОСОВ_ПРИ_РЕГИСТРАЦИИ_ПАЦИЕНТА_В_ПРИЛОЖЕНИИ.docx` (разбор — `extracted/registration-questions.txt`) | Анкета регистрации пациента: 14 вопросов, часть — списки с фиксированными вариантами, часть помечена «лучше заполнять врачу». Заявлена как основа и для статистического анализа | применено, см. [ADR-0007](../adr/0007-patient-intake-and-seizure-diary.md) |
| `дневник приступов при КД.docx` (разбор — `extracted/seizure-diary-ru.txt`) | Русский перевод дневника приступов: коды типов (A, C, F, FG, M, T, TC, O), учёт по четырём частям суток, итог за день и за месяц. Подготовлен при поддержке Danone Nutricia | применено, см. [ADR-0007](../adr/0007-patient-intake-and-seizure-diary.md) |
| `SZ DIARY KD.pdf` | Оригинал того же дневника — «KETO-STEP · Diario delle Crisi», итальянский, 8 страниц. Источник русского перевода | разобрано |

Сопроводительное сообщение заказчика: подпункты идут к главным пунктам, ввод —
«из окна в окно»; часть полей печатают родители, часть вводит админ или врач,
«чтобы более объективно было (родители не всегда правильно запоминают диагноз,
тип приступа и т. д.)».

### Поступление 06.09.2026

Три файла с замечаниями по стенду и ответами медицинской команды. **Все три
лежат в `private/`** — то есть в git не попадают, и это осознанно:

| Файл в `private/` | Что внутри | Почему не в git |
|---|---|---|
| `KDC-скриншоты-с-реальными-пациентами.docx` | как устроена KetoDietCalculator, шесть комментариев | на первом снимке список пациентов с настоящими ФИО и датами рождения |
| `письмо-и-снимки-кабинета-06.09.2026.docx` | снимки нашего кабинета и посадочной, восемь комментариев, плюс письмо о развитии проекта | имя врача-заказчицы в шапке кабинета, адрес боевого стенда и идентификатор пациента в адресной строке; репозиторий публичный |
| `пометки-на-снимках-кабинета-06.09.2026.docx` | вырезки наших экранов с надписями поверх, пять пометок | данных пациентов нет (карта тестовая), убран за компанию: снимки того же кабинета |

**Открывать их ради работы не нужно.** Всё, что в них сказано, разобрано текстом:

- [`docs/CLIENT_FEEDBACK_2026-09-06.md`](../CLIENT_FEEDBACK_2026-09-06.md) — что
  пришло и что каждая просьба значит для системы;
- [`docs/PLAN_CLIENT_FEEDBACK_2026-09.md`](../PLAN_CLIENT_FEEDBACK_2026-09.md) —
  что из этого делаем, чего не делаем и в каком порядке;
- [`extracted/feedback-2026-09-09.md`](extracted/feedback-2026-09-09.md) — ранний
  разбор одного файла из трёх; оставлен ради истории и помечен как неполный.

**Комментарии в `.docx` лежат отдельной частью файла** (`word/comments.xml`) и в
видимый текст не попадают: `textutil` их не покажет. На этом однажды уже
споткнулись — восемь из четырнадцати требований пришли именно из комментариев.

