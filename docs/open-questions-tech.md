# Открытые вопросы: технический разбор

Спутник [`docs/medical/OPEN_QUESTIONS.md`](medical/OPEN_QUESTIONS.md). Тот файл написан для
медицинской команды и намеренно не содержит имён файлов, функций и переменных: врачи не
должны собирать ответ по коду. Здесь — обратная сторона: где живёт допущение, что
изменится от ответа и что придётся переписать.

**Нумерация общая** и меняться не должна: на неё ссылаются комментарии в коде
(`# вопрос 24 в docs/medical/OPEN_QUESTIONS.md`). Номер, у которого не осталось вопроса,
переезжает в раздел «Отвеченные», а не переиспользуется.

Разбор в `docs/medical/` не положить: хуки разрешают там править только сам
`OPEN_QUESTIONS.md` (правило 1 `CLAUDE.md`), остальное — за медицинской командой.

## Что изменилось 09.09.2026

Клиника ответила на 33 вопроса из 46. Ответы дословно — в
[`OPEN_QUESTIONS.md`](medical/OPEN_QUESTIONS.md) под каждым вопросом. Здесь строки
переписаны под ответ: колонка «что меняется» стала колонкой «что делать».

**Три ответа меняли расчётное ядро, и сделаны одной работой:** вопросы 2, 3 и 6
вместе означают переход соотношения на **чистые углеводы** (за вычетом клетчатки),
при том что **лимит углеводов в назначении остаётся по общим**. Сделано 10.09.2026 —
[ADR-0030](adr/0030-net-carbs-ratio.md): `ENGINE_VERSION` 1.0.0, пересчитаны 13
предварительных эталонов, переключатель `net_carbs` убран из ядра и из схемы API.
Уже сохранённые расчёты остались посчитанными по прежнему правилу и несут прежнюю
версию ядра — снимок фиксирует то, что было.

**Один ответ ставит под сомнение принятое допущение:** вопрос 24. Клиника делит
суточную норму НЕ поровну («на обед и ужин больше, на перекусы меньше») и предлагает
произвольную форму. Наша подстановка «÷ число приёмов» остаётся подсказкой в
редактируемом поле — это не противоречит ответу, но перестало быть тем, «как считает
клиника».

## Карта

| №   | Где допущение                                                                                                | Текущее значение                                                                                                                                  | Что меняется от ответа                                                                                                                                                                                                                      |
| --- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `keto_engine/constants.py`; порог расхождения — `apps/api/.../product_checks.py`                             | **ответ 09.09.2026: оставить 9/4/4**                                                                                                              | по расчёту ничего не меняется. Отдельно: клиника просит предупреждать при расхождении «более 5 ккал» — это НЕ порог вопроса 43 (треть и 25 ккал), и цифра названа как пример («например»); переспросить, прежде чем менять                  |
| 2   | `keto_engine.engine.verify()`                                                                                | **закрыт 10.09.2026: соотношение считается по чистым углеводам**                                                                                  | сделано ([ADR-0030](adr/0030-net-carbs-ratio.md)): клетчатка вычитается по каждому продукту и не уходит в минус, `NET_CARBS_DEFAULT` и `Targets.net_carbs` удалены, `ENGINE_VERSION` 1.0.0, эталоны пересчитаны                             |
| 3   | `keto_engine.solve()`, `Targets.carbs_max_g`                                                                 | **закрыт 10.09.2026: лимит — по ВСЕМ углеводам**, при этом соотношение по чистым                                                                  | сделано: расхождение намеренное и закреплено двумя тестами решателя — лимит режет по общим (`test_carbs_limit_counts_all_carbs`), равенство соотношения строится на чистых (`test_solver_targets_the_ratio_on_net_carbs`)                   |
| 4   | миграции `688c4b9bd8b0`, `086d4c5d6d03`, `bce695d76e00`                                                      | 10 типов приступов, латинские коды дневника KETO-STEP                                                                                             | при переходе на ILAE 2025 — новая миграция справочника и правка сетки дневника (ширина колонки под ГТКП/ФППБТК)                                                                                                                             |
| 5   | `apps/api/.../prescriptions.py`, `keto_engine.max_non_fat_grams`                                             | отклоняется только арифметически невыполнимое                                                                                                     | новые границы → валидация в схеме `PrescriptionCreate` + режим «предупредить и подтвердить» (назначения append-only)                                                                                                                        |
| 6   | `apps/api/src/api/services/product_import.py`                                                                | **закрыт 10.09.2026 частично: клетчатка входит в общие углеводы, но не в чистые**                                                                 | правило импорта (`fiber > carbs` — отказ) верно и остаётся; ядро на эту проверку не полагается. Но ответ говорит о РАСЧЁТЕ, а не о конвенции источника: останется ли `carbs_100g` брутто у местной таблицы — новый вопрос 47                |
| 7   | `apps/api/src/api/routers/clinical.py` (`_DOCTOR_ONLY`), медпрофиль и препараты                              | **ответ 09.09.2026: диетолог ВИДИТ диагноз, календарь приступов и назначенные препараты, но не правит**                                           | медпрофиль разделить на читаемое диетологом и закрытое; запись оставить врачу. Тесты 403 на каждую задетую ручку, `safety-reviewer` обязателен                                                                                              |
| 8   | `core/models/enums.py`, таблица `ketone_methods`                                                             | enum `blood`/`urine` + неиспользуемый справочник                                                                                                  | «методов больше двух» → миграция `ketone_logs.method` в FK                                                                                                                                                                                  |
| 9   | `apps/api/.../overview.py`, `apps/web/src/features/patients/dayVerdict.ts`                                   | соотношение — предупреждение, калорийность — прогресс                                                                                             | вариант 1 требует признака завершённости дня (в ТЗ его нет), вариант 2 — доли от `meals_per_day`                                                                                                                                            |
| 10  | `keto_engine/constants.py`, `docs/medical/reference-cases/`                                                  | значения ТЗ §6.2 с `# TODO(med)`, 35 эталонов `provisional: true`                                                                                 | сверка со спецификацией, замена эталонов, снятие `provisional`, bump `ENGINE_VERSION`                                                                                                                                                       |
| 11  | `apps/web/src/features/doctor/flags.ts`                                                                      | `NO_DATA_FLAG_DAYS = 3`, в интерфейсе пометка «предварительный»                                                                                   | одно число; при разных порогах для кетонов и веса — две константы и правка `PatientFlagsView`                                                                                                                                               |
| 12  | флаг не реализован                                                                                           | —                                                                                                                                                 | нужны и агрегация (`entries` против `count` в ответе сервера), и порог; подменять одно другим нельзя                                                                                                                                        |
| 13  | `apps/api/src/api/services/menus.py`                                                                         | множитель 1 = вся раскладка рецепта                                                                                                               | «одна порция» → делить на `servings` при подстановке, миграции не нужно                                                                                                                                                                     |
| 14  | `apps/web/src/features/diary/schemas.ts`                                                                     | `KETONE_MIN_MMOL`/`KETONE_MAX_MMOL` одни на оба метода                                                                                            | отдельные границы + единица в ответе API                                                                                                                                                                                                    |
| 15  | не реализовано                                                                                               | `weight_logs` хранит вес и рост без оценки                                                                                                        | таблицы ВОЗ/национальные — это медицинские данные (правило 1), внести без ответа нельзя                                                                                                                                                     |
| 16  | не реализовано                                                                                               | добавки неотличимы от ПЭП в `medications`                                                                                                         | справочник добавок либо тип записи в `medications` — миграция                                                                                                                                                                               |
| 17  | не реализовано                                                                                               | флаг показывает молчание семьи, не пропущенный визит                                                                                              | нужны дата старта терапии и модель визитов                                                                                                                                                                                                  |
| 18  | не реализовано                                                                                               | признака завершения диеты нет                                                                                                                     | поле состояния пациента + фильтры во всех списках врача                                                                                                                                                                                     |
| 19  | миграция `bce695d76e00`, `intake_options`                                                                    | шкала заказчика + «реже раза в 3 месяца», «приступов нет», «оценить не удаётся»                                                                   | единица счёта (дни с приступами против числа приступов) определяет, можно ли сводить анкету с дневником                                                                                                                                     |
| 20  | миграция `bce695d76e00`                                                                                      | длительность по границам ILAE (Trinka 2015), препараты 0/1/2/3-5/6+                                                                               | правка формулировок — новая миграция справочника                                                                                                                                                                                            |
| 21  | миграция `bce695d76e00`, `aed_drugs`                                                                         | вещество + синонимы, 30 позиций                                                                                                                   | правка перечня — миграция; переименование ПЭП → ASM затрагивает i18n                                                                                                                                                                        |
| 22  | `docs/medical/reference-cases/verify_ratio_*.yaml`                                                           | имя файла и `note` расходятся с `expected.ratio`                                                                                                  | правит медицинская команда: `docs/medical/*` защищён хуком                                                                                                                                                                                  |
| 23  | `apps/bot/.../scenarios.py` (`Seizure`), `seizure_logs.duration_option_id`, ADR-0020                         | шкала анкеты (`intake_options`, `seizure_duration`) у бота, кабинета и анкеты одна; интервал хранится ссылкой, `duration_sec` — только измеренное | другая шкала — правка справочника миграцией, кода не касается; отдельное деление на 2–3 минутах (КР РФ 2022) — новый вариант справочника; «обычная длительность у этого ребёнка» (NICE NG217 7.3.1) — новое поле анкеты и правило сравнения |
| 24  | `packages/ui/src/lib/mealTargets.ts`                                                                         | **ответ 09.09.2026: делить поровну не обязательно, «предлагаю произвольную форму»**                                                               | поле уже редактируемое — оставляем; подстановка «÷ число приёмов» становится подсказкой, а не правилом клиники. Комментарий `TODO(med)` переписать под ответ                                                                                |
| 25  | `ATTACHMENT_PURGE_DAYS` (по умолчанию 30), `worker.maintenance.purge_files`                                  | 30 дней по аналогии со сроком дампов                                                                                                              | одна переменная окружения; «стереть немедленно» отменяет отсрочку целиком                                                                                                                                                                   |
| 26  | `product_categories`, колонка `category` в CSV                                                               | ручка создания категории и нормализованный уникальный индекс на `name_ru` уже есть; списка от клиники нет                                         | перечень от клиники → сид-миграция справочника                                                                                                                                                                                              |
| 27  | `apps/api/src/api/services/product_import.py`                                                                | сумма макросов `> 100 г` — отказ строки, файл не грузится целиком                                                                                 | допуск 0,5 г — одна константа; проверить, что перепутанные колонки по-прежнему ловятся (расхождение в разы)                                                                                                                                 |
| 28  | `menu_items.meal_index`; подпись «Приём k» в пяти местах                                                     | **ответ 09.09.2026: число приёмов не ограничивать** (ADR-0029 подтверждён)                                                                        | имён приёмов клиника не назвала: перечислила РАЗДЕЛЫ РЕЦЕПТОВ (завтрак, обед, ужин, перекус, десерты, напитки, выпечка, хлеб) — это про `recipes.category`, а не про план дня. Переспросить отдельно                                        |
| 29  | `apps/api/src/api/routers/calc.py`, экран меню                                                               | `solve` снимает исключённые со входа, `verify` только называет                                                                                    | запрет → 409 на сохранении приёма пищи и обход через «записать вне плана»                                                                                                                                                                   |
| 30  | не реализовано                                                                                               | **ответ 09.09.2026: коридор семье не показывать**, оценивают врач и диетолог; измеряют по моче, шкала лакмуса своя                                | родителю коридор не рисуем вовсе. Нужна шкала кетонов в моче (ответ 14 — «пришлю фото»); до неё оценка невозможна                                                                                                                           |
| 31  | `apps/api/src/api/routers/prescriptions.py`, `canWritePrescriptions`                                         | **ответ 09.09.2026: авторы назначения — врач И диетолог**                                                                                         | текущее поведение подтверждено; диетолог не назначает лекарства и видит назначенные — это часть работы по вопросу 7                                                                                                                         |
| 32  | `RecipeState.REVIEWED` (`core/models/enums.py`)                                                              | состояние объявлено, перевести в него нечем                                                                                                       | нужен переход и роль рецензента либо удаление состояния из перечисления (миграция)                                                                                                                                                          |
| 33  | `apps/api/.../invitations.py`, отключение специалиста (409)                                                  | второго родителя приглашает только администратор; передача пациентов не решена                                                                    | «семья приглашает сама» — новая ручка и правило доступа; передача — массовая операция у администратора                                                                                                                                      |
| 34  | не реализовано                                                                                               | лабораторных показателей нет ни в схеме, ни в отчёте                                                                                              | новая таблица + миграция, экран у врача, блок в отчёте; границы нормы по возрасту — медицинские данные (правило 1)                                                                                                                          |
| 35  | `apps/bot/.../scenarios.py`, `seizure_logs.count`                                                            | бот пишет `count = 1`, отдельная запись на приступ; в кабинете поле есть                                                                          | шаг «сколько подряд» — четвёртый шаг сценария (ТЗ §7.3 допускает 2–4); «серия отдельным типом» — миграция справочника типов                                                                                                                 |
| 36  | `apps/api/.../ai_conversations.py`                                                                           | переписку читают врач и диетолог, админу закрыта; дисклеймер нейтральный продуктовый                                                              | «только врач» — сужение роли в ручке и тест 403; формулировка клиники — строка в `apps/web/src/locales/ru/assistant.json` и в `packages/ui`                                                                                                 |
| 37  | `packages/core/.../reports.py` (`medication_adherence`), `medications.frequency`                             | считается «отмечено / из них не принято»; доли пропущенных нет — знаменателя не существует                                                        | поле «приёмов в сутки» числом → миграция `medications` + правка формы у врача + новое число в нагрузке сводки                                                                                                                               |
| 38  | `core/textguard/summary_guard.py` (`HARD_KINDS`)                                                             | жёсткие — рекомендация, причина, диагноз, сбой фильтра; мягкие — структура и выдуманное число                                                     | перенос класса между жёсткими и мягкими — одна строка `HARD_KINDS`; на экране меняется только текст объяснения                                                                                                                              |
| 39  | `core/textguard/summary_lexicons.py` (`CAUSAL`)                                                              | «после» разрешено, «из-за» запрещено                                                                                                              | смягчение — вынуть связку из списка; ужесточение — добавить «после» в паре с датой, тогда нужен новый кейс в корпусе                                                                                                                        |
| 40  | `apps/api/.../summaries.py` (`_require_doctor`)                                                              | сводку заказывает и утверждает только врач                                                                                                        | допуск диетолога — правка одной проверки и тестов 403; чтение и утверждение можно развести                                                                                                                                                  |
| 41  | `apps/api/.../summaries.py` (`MAX_PERIOD_DAYS = 92`)                                                         | квартал — обычный интервал контрольного визита                                                                                                    | другое число — одна константа; сильно больший период потребует более грубой гранулярности рядов в нагрузке                                                                                                                                  |
| 42  | `apps/api/.../reports.py` (`_is_specialist`), `services/reports.py` (`with_summaries`)                       | сводки собираются в отчёт только врачу и диетологу                                                                                                | «семья видит» — снять условие; «видит в другой формулировке» — отдельное поле, а не тот же `approved_md`                                                                                                                                    |
| 43  | `apps/api/.../product_checks.py` (`KCAL_MISMATCH_FRACTION`, `KCAL_MISMATCH_MIN`)                             | треть расхождения, но не меньше 25 ккал; коэффициенты 9/4/4 берутся из `keto_engine.constants`                                                    | другой порог — две константы; на расчёт меню не влияет, только на список подозрительных у администратора                                                                                                                                    |
| 44  | `Medication.dose` — строка; форма `MedicationForm.tsx`                                                       | доза как есть («300 мг»), единицы нет                                                                                                             | одна единица → подпись у поля; выбор единицы → колонка `dose_unit` (миграция) и правка формы; в обоих случаях по дозе по-прежнему ничего не считается                                                                                       |
| 45  | `Medication.frequency` — строка                                                                              | свободный текст                                                                                                                                   | список значений → справочник или enum + миграция уже записанных строк; до ответа список закрывать нельзя                                                                                                                                    |
| 46  | `keto_engine/constants.py`: `RATIO_TOLERANCE = 0.15`, `KCAL_TOLERANCE_FRACTION = 0.05`, `within_tolerance()` | допуск симметричный по обеим величинам                                                                                                            | односторонний допуск по соотношению → правка `within_tolerance`, bump `ENGINE_VERSION` (major: меняет вердикт уже сохранённых расчётов) и пересчёт эталонов; вопрос 10 закрывается вместе с этим                                            |
| 47  | `apps/api/.../product_import.py` и `schemas.py` (`fiber ≤ carbs`), `infra/seed/README.md`                    | `carbs_100g` — углеводы ВМЕСТЕ с клетчаткой (конвенция USDA); держит это одна проверка импорта                                                    | источник с раздельным учётом → вычитание в ядре сработает дважды и завысит соотношение. Если местная таблица окажется такой, нужна колонка «углеводы уже без клетчатки» или пересчёт при загрузке; молча принимать нельзя                    |

## Правила работы с этим списком

1. **Ответ фиксируется дважды**: в `OPEN_QUESTIONS.md` (раздел «Отвеченные», с датой и
   именем) и в коде — комментарием со ссылкой на номер вопроса вместо `# TODO(med)`.
2. **Ответ, меняющий математику**, — это bump `ENGINE_VERSION` и правка эталонов
   (правило 2 `CLAUDE.md`). Эталон правится только со ссылкой на версию медицинской
   спецификации.
3. **Ответ, меняющий справочник**, — новая миграция, а не правка старой (правило 3).
4. **Ответ, меняющий доступ**, — новые тесты 403 на каждую задетую ручку (правило 5).
5. Отступление от ТЗ ради ответа медкоманды оформляется ADR в `docs/adr/`.
