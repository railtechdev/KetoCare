/**
 * Задержка автоматического пересчёта — одна на все экраны с живым расчётом.
 *
 * Правка граммовки — это несколько нажатий подряд; без задержки каждое
 * уходило бы в расчёт. Калькулятор кабинета, калькулятор Mini App и форма
 * рецепта объявляли её каждый у себя и однажды разошлись бы, а одна и та же
 * семья видит калькулятор в двух местах (ADR-0028). Тесты читают её отсюда,
 * чтобы дождаться, что лишняя проверка точно успела бы уйти.
 */
export const RECALC_DELAY_MS = 400;
