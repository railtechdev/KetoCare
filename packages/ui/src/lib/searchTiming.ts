/**
 * Задержка поискового поля — одна на все поиски кабинета и Mini App.
 *
 * Запрос уходит, когда набор стоит спокойно: без задержки каждый символ шёл бы
 * полнотекстовым запросом к базе. Кабинет держал 300 мс литералом в семи
 * местах, а поиск продукта (калькулятор, форма рецепта, исключённые продукты
 * ребёнка) шёл вовсе без задержки — каждой буквой. Mini App ждал 400 мс:
 * калькулятор делил константу с пересчётом, рецепты держали свой литерал.
 * Поиск один и тот же, и одна семья видит его в двух местах, поэтому значение
 * одно — 300 мс, как у большинства полей. От задержки пересчёта (`RECALC_DELAY_MS`) оно отдельно:
 * правка граммовки и набор названия — разные действия, совпадать им незачем.
 */
export const SEARCH_DELAY_MS = 300;
