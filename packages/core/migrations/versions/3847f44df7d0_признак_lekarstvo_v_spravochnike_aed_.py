"""признак «это лекарство» в справочнике aed_drugs

Revision ID: 3847f44df7d0
Revises: 82861bb2d318
Create Date: 2026-09-10 16:43:35.368068

В `aed_drugs` живут не только препараты. Справочник заводился под анкету семьи
(«какие препараты принимает»), и вариантами ответа там же стоят «Другое
(указать)», «Не принимает противоэпилептические препараты» и «Не знаю
названия» — три последние строки сида.

Пока справочник читала только анкета, это ничему не мешало: варианты ответа там
и должны быть. Но схема лекарственной терапии стала подсказывать названия из
того же справочника, и врач, набравший «не», получал в подсказке «Не знаю
названия» — а выбор подставлял эту строку в `drug_name` как каноническое
название препарата.

**Почему признаком, а не списком имён в коде.** Имя — не идентификатор.
У `aed_drugs`, в отличие от `intake_options`, нет стабильного `code`, поэтому
отсев по названию цеплялся бы за отображаемую строку: переименуют — и «Не знаю
названия» снова станет препаратом, молча.

Данные помечает эта же миграция: три строки известны поимённо, потому что их
завёл наш сид (`bce695d76e00`) и он же переутверждает эти имена при каждом
прогоне (`ON CONFLICT (name_ru) DO UPDATE`). Всё остальное — препараты, поэтому
умолчание `true`, а не `false`.

Если помечено не три строки, миграция ПАДАЕТ. `UPDATE ... WHERE name_ru = ANY`
на несовпадении обновил бы ноль строк и завершился успешно — то есть служебные
строки остались бы предлагаться врачу как лекарства, и никто бы не узнал.

Падение безопасно для стенда: `deploy.sh` гонит миграции до пересоздания
приложений, и при отказе там продолжает работать прежняя версия. Сообщение
говорит, что делать дальше, — и это НЕ правка этого файла.
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "3847f44df7d0"
down_revision: str | None = "82861bb2d318"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None

#: Строки сида, которые называют не лекарство, а вариант ответа анкеты.
_NOT_DRUGS = (
    "Другое (указать)",
    "Не принимает противоэпилептические препараты",
    "Не знаю названия",
)


def upgrade() -> None:
    op.add_column(
        "aed_drugs",
        sa.Column("is_drug", sa.Boolean(), server_default=sa.text("true"), nullable=False),
    )
    marked = (
        op.get_bind()
        .execute(
            sa.text(
                "UPDATE aed_drugs SET is_drug = false WHERE name_ru = ANY(:names) RETURNING name_ru"
            ).bindparams(sa.bindparam("names", value=list(_NOT_DRUGS), type_=sa.ARRAY(sa.String)))
        )
        .fetchall()
    )

    found = {row[0] for row in marked}
    missing = set(_NOT_DRUGS) - found
    if missing:
        raise RuntimeError(
            "Не найдены служебные строки справочника препаратов: "
            f"{sorted(missing)}. Их переименовали в базе?\n"
            "\n"
            "Пока они помечены как лекарства, подсказка в схеме лечения "
            "предлагает их врачу наравне с препаратами.\n"
            "\n"
            "Что делать: НЕ правьте эту миграцию — она закоммичена (правило 3 "
            "CLAUDE.md). Заведите новую ревизию, которая проставит "
            "`is_drug = false` строкам с их нынешними именами, и приложите "
            "выборку `SELECT id, name_ru FROM aed_drugs ORDER BY sort` к PR.\n"
            "\n"
            "Выкат при этом не сломан: `deploy.sh` гонит миграции ДО пересоздания "
            "приложений, поэтому на стенде продолжает работать прежняя версия."
        )


def downgrade() -> None:
    op.drop_column("aed_drugs", "is_drug")
