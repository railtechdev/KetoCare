# Prettier форматирует только исходники приложений и пакетов (`src` каждого
# workspace) — так проверяет `make lint`, и `make fix` обязан совпадать с ним.
#
# Этот файл — вторая защита. Однажды `make fix` прошёлся `prettier --write .` по
# всему репозиторию и переписал 34 эталонных случая в `docs/medical`, файлы
# `.claude/` и `pnpm-lock.yaml` на 6691 строку. Правило 2 CLAUDE.md: эталоны
# меняет медицинская команда — даже переносом отступов.

# Генерируется пакетным менеджером
pnpm-lock.yaml

# Медицинские материалы: правит медицинская команда (правила 1 и 2 CLAUDE.md)
docs/medical/

# Инструментарий агента: защита, которую агент переписывает сам, — не защита
.claude/

# Генерируется из OpenAPI командой `make openapi`
packages/api-client/src/generated/
apps/api/openapi.json

# Присланные заказчиком исходники — как есть, включая разбор
docs/client/

# Сборка и зависимости
node_modules/
dist/
coverage/
