# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.idea/
.vscode/

# Build artifacts
/infra/backups/
apps/api/openapi.json
packages/api-client/src/generated/

# Локальные оверрайды docker compose (напр., другие порты при конфликте на машине разработчика)
infra/docker-compose.override.yml

# Материалы заказчика с реальными данными пациентов: агент их читает, но в
# репозиторий они не попадают (раздел 16 ТЗ — не коммитить реальные ФИО)
docs/client/private/

# Том под собранные отчёты (раздел 7.5 ТЗ): файлы с клиническими данными
var/

# Граф знаний по репозиторию (/graphify): выводится из исходников, 15 МБ,
# пересобирается командой заново.
graphify-out/
